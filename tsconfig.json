{
  "compilerOptions": {
    /* Visit https://aka.ms/tsconfig.json to read more about this file */

    /* Projects */
    "incremental": true,                              /* Enable incremental compilation for faster builds. */
    
    /* Language and Environment */
    "target": "es2020",                               /* Set the JavaScript language version to ES2020. You can still use newer syntax with Babel if needed. */
    "lib": ["dom", "es2020"],                         /* Specify the libraries for the runtime environment. */
    "jsx": "react-jsx",                               /* Use the new JSX transform for React (if React 17+). */
    "experimentalDecorators": true,                   /* Enable experimental support for decorators if used in the project. */
    "emitDecoratorMetadata": true,                    /* Emit metadata for decorators if needed. */
    "useDefineForClassFields": true,                  /* Emit ECMAScript-compliant class fields. */

    /* Modules */
    "module": "esnext",                               /* Use ESNext module syntax for tree-shaking and modern imports. */
    "moduleResolution": "node",                       /* Resolve modules in Node.js-style (default). */
    "resolveJsonModule": true,                        /* Enable importing JSON files as modules. */
    "esModuleInterop": true,                          /* Allow interoperability between CommonJS and ES6 modules. */
    "forceConsistentCasingInFileNames": true,         /* Ensure case-sensitive file imports. */
    "allowSyntheticDefaultImports": true,             /* Allow default imports from modules with no default export. */

    /* Type Checking */
    "strict": true,                                   /* Enable all strict type-checking options. */
    "noImplicitAny": true,                            /* Enable error reporting for variables with an implicit `any` type. */
    "strictNullChecks": true,                         /* Ensure `null` and `undefined` are properly checked. */
    "strictBindCallApply": true,                      /* Ensure parameters for `bind`, `call`, and `apply` match original functions. */
    "strictFunctionTypes": true,                      /* Ensure type safety when assigning function types. */
    "strictPropertyInitialization": true,             /* Ensure all class properties are initialized. */
    "noImplicitThis": true,                           /* Ensure `this` is not implicitly `any`. */
    "noUnusedLocals": true,                           /* Enable error reporting for unused local variables. */
    "noUnusedParameters": true,                       /* Enable error reporting for unused parameters. */
    "noFallthroughCasesInSwitch": true,               /* Prevent fallthrough in switch statements. */
    "noImplicitReturns": true,                        /* Ensure all code paths in functions return a value. */

    /* Completeness */
    "skipLibCheck": true                              /* Skip type checking of .d.ts files for faster compilation. */
  },
  "include": [
    "./src/**/*",
    "./gatsby-node.ts",
    "./gatsby-config.ts",
    "./plugins/**/*"
  ],
  "exclude": [
    "node_modules",                                   /* Exclude dependencies from type checking. */
    ".cache",                                         /* Exclude Gatsby's cache folder. */
    "public"                                          /* Exclude Gatsby's public folder. */
  ]
}
